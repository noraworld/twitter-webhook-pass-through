# Twitter Webhook Pass-through
This application retrieves the Twitter account activities via Twitter Activity API, and gives them to several internal services or bots as is.

## Usage
```shell
$ npm install
$ node index.js
```
